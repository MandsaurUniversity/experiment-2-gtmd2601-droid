// Lab 20: JavaScript Dynamic Styling

// Helper: Get the currently selected paragraph id
// function getSelectedPara() {
//     var radios = document.getElementsByName("paraSelect");
//     for (var i = 0; i < radios.length; i++) {
//         if (radios[i].checked) {
//             return radios[i].value;
//         }
//     }
//     return "para1";
// }

// TODO: Write functions to dynamically style text

function toggleBold(paraId) {
    // var para = document.getElementById(paraId);
    // TODO: Toggle bold on the paragraph
    // Hint: Check if para.style.fontWeight === "bold"
}

function toggleItalic(paraId) {
    // TODO: Toggle italic on the paragraph
}

function toggleUnderline(paraId) {
    // TODO: Toggle underline on the paragraph
}

function resetAll() {
    // TODO: Reset all paragraphs to default styling
}

// Bonus: Change text color
// function changeColor(paraId, color) { ... }
